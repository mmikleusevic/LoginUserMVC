# LoginUserMVC

#CREATE TABLE [dbo].[tblUser]( [ID] [int] IDENTITY(1,1) NOT NULL, [FirstName] varchar NULL, [LastName] varchar NULL, [Oib] varchar NULL, [Email] varchar NULL, [UserName] varchar NULL, [Password] varchar NULL, [DoB] [date] NULL

Username = user, Password = user
